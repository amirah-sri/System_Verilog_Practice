 1.Write a SystemVerilog code snippet that demonstrates functional coverage
 for a 4-bit counter that should cover all possible values from 0 to 15.
 
2. How would you implement functional coverage for a packet-based protocol
 where the packet can have various types, priorities, and lengths? Create a
 SystemVerilog code snippet that captures this scenario
 
 3.   Cover even and odd muliples of 10 within 0:100
 
 4. Write a SystemVerilog assertion to check that the signal data_ready goes
 high within 3 cycles after data_valid goes high, and if it does not, flag an
 error.
 
 5.How do you measure the toggle coverage of a bus in SystemVerilog? Create
 a code snippet that demonstrates toggle coverage for a 4-bit bus.
 Explanation:
 This covergroup captures toggle coverage on each bit of a 4-bit bus by
 setting bins for 0 and 1.
 The cross of all bits ensures all possible toggles and combinations are
 monitored.
 6. Write an assertion to verify that ack must be low for at least one cycle before it can go high again after being asserted
 
 7.How would you implement coverage to check all possible sequences of two
 signals req and ack in a SystemVerilog testbench?
 Explanation:
 This covergroup includes two sequences: seq_req_ack and seq_ack_req.
 The coverpoint req_ack_seq has bins for both sequences, ensuring all
 possible orderings of req and ack are monitored.
 Sampling on each clock allows capturing these sequences when they occur
 during simulation (could not understand)
 
 8.How would you implement an assertion to check that a ready signal goes high
 within 5 cycles after reset goes low?
 [0:3] -> within 3 clock cycle
 [*3]-> consicutive 3 clock cycle
 
 
 9. Create an assertion to verify that if req goes high, ack must follow within 3
 cycles.
 
 10. Create an assertion to check that a signal valid remains high for at least 3
 consecutive cycles once it goes high

 